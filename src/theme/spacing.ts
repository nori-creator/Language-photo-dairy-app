/** 4pt spacing grid, matching Apple's layout rhythm. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

/** Standard iOS corner radii. */
export const radius = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 22,
  card: 20,
  full: 999,
};

/** Minimum tappable target per HIG. */
export const HIT_TARGET = 44;
